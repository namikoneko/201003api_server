CREATE TABLE post (
id integer primary key autoincrement,
date text,
catid integer,
title text,
text text,
updated text
);
CREATE TABLE cat (
id integer primary key autoincrement,
date text,
title text,
text text,
updated text
);
